<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mob.welups">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_welups"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_welups"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.container.MainActivity"
            android:exported="true" />

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.home.settings.SettingAct"/>

        <activity android:name=".ui.home.generate_account.GenerateAccountAct"/>

        <activity android:name=".ui.home.wallet_manager.WalletManagerAct"/>

        <activity android:name=".ui.qr_scanner.ScannerQRCodeFragment"/>

        <activity android:name=".ui.login.LoginActivity"/>

        <activity android:name=".ui.pincode.PinCodeAct"/>

        <activity
            android:name=".ui.onboarding.OnBoardingActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />
    </application>
</manifest>